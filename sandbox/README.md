Open sandbox HTML files directly in a browser (double-click or drag into a tab). No build step or local server required.

- **`priority-variants.html`** : Baseline formula variant (A), compact 3-slider UI; shared logic in `priority-shared.js` / `priority-shared.css`

### Formula (priority-variants.html)

| | Formula | Model |
|---|---|---|
| **A** | `pression + 0.8×Impact×Facilité÷5` | Baseline (merged urgency) |

Each field has a `?` button opening a modal with keyword hints and scale-level descriptions.
