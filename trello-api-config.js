/* Trello Power-Up REST API key — generate at trello.com/power-ups/admin → your Power-Up → API Key. */
(function (global) {
  'use strict';

  global.TrelloApiConfig = {
    appKey: '',
    appName: 'Priorité',
  };
})(typeof window !== 'undefined' ? window : this);
